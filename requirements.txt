# Deploy-friendly requirements (omitted packages that often require C builds on cloud)
# wordcloud and gensim removed to avoid source build failures on some cloud runtimes.
numpy==1.26.4
pandas==2.1.3
scikit-learn==1.3.0
nltk==3.8.1
matplotlib==3.7.2
seaborn==0.12.2
# wordcloud==1.9.2  # commented out due to build issues on some cloud images
# gensim==4.3.1    # commented out; enable only if runtime supports building wheels
textblob==0.17.1
vaderSentiment==3.3.2
streamlit==1.28.0
plotly==5.17.0